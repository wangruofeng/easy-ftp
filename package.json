{
  "name": "easy-ftp",
  "version": "1.0.1",
  "description": "FTP server with Pieter-style web index (anonymous FTP + HTTP directory listing)",
  "license": "MIT",
  "main": "src/index.js",
  "scripts": {
    "start": "nodemon --watch src --watch config.js --watch .env --ext js,json,env --ignore data/ --exec \"node src/index.js\"",
    "serve": "node src/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "ftp-srv": "^4.6.2",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
